- make my 'server' recognized as 'server' and resolve across all devices in network

- Make working bootable nixos iso
- Test isos using qemu or some other tool. I can't just be waiting all day to burn it onto the usb and testing it via ventoy
